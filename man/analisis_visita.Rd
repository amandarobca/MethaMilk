% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analisis_visita.R
\name{analisis_visita}
\alias{analisis_visita}
\title{Análisis descriptivo y gráfico de una visita concreta}
\usage{
analisis_visita(
  base,
  data_sniffer,
  id_vaca,
  fecha,
  n_visita = NULL,
  grafico = FALSE,
  mostrar_suavizado = FALSE,
  mostrar_background = TRUE,
  mostrar_auc = FALSE,
  delay_seg = 25,
  prob_background = 0.2,
  min_altura = 0.02,
  min_distancia = 5,
  suavizado = 7,
  truncar_neg_auc = FALSE
)
}
\arguments{
\item{base}{Base unificada (salida de \code{\link[=unir_bases]{unir_bases()}}).}

\item{data_sniffer}{Sniffer ya limpio (salida de \code{\link[=limpiar_sniffer]{limpiar_sniffer()}}).}

\item{id_vaca}{Identificador de la vaca.}

\item{fecha}{Fecha (o fecha y hora) de inicio de la visita, como texto. Puede
ser parcial: \code{"2021-03-17"} selecciona todas las visitas de ese día y
\code{"2021-03-17 03:29"} una concreta.}

\item{n_visita}{Si \code{fecha} corresponde a varias visitas, índice (1, 2, …) de
la que se desea. Por defecto \code{NULL}.}

\item{grafico}{Si es \code{TRUE}, devuelve además el gráfico de la serie. Por
defecto \code{FALSE}.}

\item{mostrar_suavizado, mostrar_background, mostrar_auc}{Interruptores de los
overlays del gráfico: señal suavizada, línea de fondo y área bajo la curva.}

\item{delay_seg, prob_background, min_altura, min_distancia, suavizado, truncar_neg_auc}{Parámetros de cálculo, con los mismos valores por defecto que \code{\link[=unir_bases]{unir_bases()}}.}
}
\value{
Si \code{grafico = FALSE}, devuelve de forma invisible una lista con el
descriptivo. Si \code{grafico = TRUE}, devuelve el objeto \code{ggplot}. En ambos
casos imprime el descriptivo por consola.
}
\description{
Localiza una visita en la base unificada a partir del identificador de la
vaca y la fecha de inicio, reconstruye su serie temporal de CH4 a partir del
sniffer (descartando los segundos solapados, igual que \code{\link[=unir_bases]{unir_bases()}}) y
devuelve un resumen descriptivo de la visita. Opcionalmente dibuja la serie
de CH4, sin marcar los picos, con superposición del suavizado, la línea de
fondo y/o el área bajo la curva.
}
