% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/limpiar_sala.R
\name{limpiar_sala}
\alias{limpiar_sala}
\title{Cargar y limpiar los datos crudos del sistema de ordeño (AMS)}
\usage{
limpiar_sala(
  archivo,
  sep = ";",
  formato_fecha = "\%d/\%m/\%Y \%H:\%M",
  fileEncoding = "latin1",
  solo_entradas = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{archivo}{Ruta al archivo CSV exportado por el AMS (por ejemplo,
\code{"Campon102_new.csv"}).}

\item{sep}{Carácter separador de campos. Por defecto \code{";"}.}

\item{formato_fecha}{Formato de fecha y hora pasado a \code{\link[=as.POSIXct]{as.POSIXct()}}.
Por defecto \code{"\%d/\%m/\%Y \%H:\%M"}, que corresponde a marcas del tipo
\code{"08/03/2021 0:07"}.}

\item{fileEncoding}{Codificación del archivo en disco. Por defecto \code{"latin1"},
que recupera correctamente los acentos de la columna \code{Descripcion} en las
exportaciones originales. Para un archivo en UTF-8, usar \code{"UTF-8"}.}

\item{solo_entradas}{Si es \code{TRUE} y el archivo incluye la columna de evento,
conserva únicamente las filas \code{"ENTRADA"}, de modo que quede una sola fila
por visita. En archivos sin columna de evento (que ya traen una fila por
visita) no tiene efecto. Por defecto \code{FALSE}.}

\item{verbose}{Si es \code{TRUE}, informa por consola del número de registros
leídos, del formato detectado y de los valores ausentes en las columnas
clave. Por defecto \code{FALSE}.}
}
\value{
Un \code{data.frame} con las columnas estandarizadas \code{id_vaca},
\code{id_robot}, \code{FechaHora} (\code{POSIXct}), \code{Tiempo_cubiculo}, \code{Produccion},
\code{Tiempo_ordeño} y \code{Descripcion}, ordenado de forma ascendente por
\code{FechaHora}. Si el archivo incluye la columna de evento y \code{solo_entradas}
es \code{FALSE}, se añade además \code{Evento} con los valores \code{"ENTRADA"}/\code{"SALIDA"}
sin espacios sobrantes.
}
\description{
Lee un archivo CSV con los registros de visitas exportados por el sistema
de ordeño automático (AMS), normaliza los nombres de las columnas, convierte
los tipos de datos y ordena las filas cronológicamente. La función admite
los dos formatos de exportación del AMS: con columna de evento
(ENTRADA/SALIDA, 8 columnas) y sin ella (7 columnas).
}
\examples{
\dontrun{
# Base con evento, conservando ambas filas por visita
sala <- limpiar_sala("Campon102_new.csv", verbose = TRUE)

# Base con evento, dejando una sola fila por visita
sala_unica <- limpiar_sala("Campon102_new.csv", solo_entradas = TRUE)
}

}
