% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unir_bases.R
\name{unir_bases}
\alias{unir_bases}
\title{Unificar los datos del AMS y del sniffer en una base por visita}
\usage{
unir_bases(
  data_sala,
  data_sniffer,
  delay_seg = 25,
  dur_min = 120,
  dur_max = 600,
  ventana_seg = 300,
  prob_background = 0.2,
  min_altura = 0.02,
  min_distancia = 5,
  suavizado = 7,
  truncar_neg_auc = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{data_sala}{Data frame del AMS ya limpio (salida de \code{\link[=limpiar_sala]{limpiar_sala()}}),
con \code{FechaHora} en formato \code{POSIXct}. Si conserva la columna \code{Evento}, se
filtran automáticamente las filas \code{"ENTRADA"}.}

\item{data_sniffer}{Data frame del sniffer ya limpio (salida de
\code{\link[=limpiar_sniffer]{limpiar_sniffer()}}).}

\item{delay_seg}{Retardo de detección, en segundos, aplicado tanto a la
detección de solapes como a la búsqueda de medidas del sniffer. Por
defecto \code{25}.}

\item{dur_min, dur_max}{Límites inferior y superior, en segundos, para
conservar una visita según su duración. Por defecto \code{120} y \code{600}.}

\item{ventana_seg}{Duración máxima, en segundos, de la ventana efectiva de
análisis a partir del inicio de la visita. Por defecto \code{300}.}

\item{prob_background}{Cuantil empleado para estimar el nivel de fondo
(background) de CH4. Por defecto \code{0.20}.}

\item{min_altura, min_distancia, suavizado}{Parámetros de la detección de
picos: altura mínima del pico, distancia mínima entre picos (en medidas) y
tamaño de la ventana de suavizado (en medidas). Por defecto \code{0.02}, \code{5} y
\code{7}, los valores óptimos obtenidos en la validación.}

\item{truncar_neg_auc}{Si es \code{TRUE}, los valores negativos de CH4 corregido
se truncan a cero antes de integrar el área bajo la curva. Por defecto
\code{FALSE} (se conservan los valores negativos).}

\item{verbose}{Si es \code{TRUE}, informa por consola del número de visitas y
medidas en cada paso. Por defecto \code{FALSE}.}
}
\value{
Un \code{data.frame} con una fila por visita y las columnas \code{id_vaca},
\code{inicio_visita}, \code{fin_analisis}, \code{tiempo_efectivo}, \code{produccion},
\code{media_CH4}, \code{media_CO2}, \code{ratio_media}, \code{auc_CH4}, \code{varianza_CH4},
\code{n_picos}, \code{picos_por_minuto} y \code{n_medidas}.
}
\description{
Cruza los registros de visitas del sistema de ordeño (AMS) ya limpios con
los registros del sniffer ya limpios, define la ventana efectiva de análisis
de cada visita, descarta los segundos cubiertos por más de una visita y
resume, por visita, las concentraciones de gases, el área bajo la curva de
CH4 corregida por el nivel de fondo, la varianza de CH4 y el número de picos
de eructación. Todo el proceso se realiza en una sola función.
}
\examples{
\dontrun{
sala    <- limpiar_sala("Campon102_new.csv")
sniffer <- limpiar_sniffer("CAMPON_102_Loggy_25sg_FD2.02.txt")
base    <- unir_bases(sala, sniffer, delay_seg = 25, verbose = TRUE)
}

}
